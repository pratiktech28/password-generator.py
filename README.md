# 🔐 Python Password Generator

A secure and customizable password generator built using Python. This CLI-based tool creates strong passwords using random combinations of letters, digits, and symbols — perfect for personal use or integrating into larger authentication systems.

---

## 🚀 Features

- 🔢 Generates passwords of custom length
- 🔀 Mix of uppercase, lowercase, digits, and special characters
- 🧠 Option to exclude similar-looking characters (like `l`, `1`, `O`, `0`)
- 🔒 Ensures randomness using Python's `secrets` and `random` modules
- 📦 Lightweight and beginner-friendly

---

## 📂 Project Structure
password-generator/ ├── password_generator.py   # Main script ├── README.md               # Documentation └── samples.txt             # Example outputs
